          <div class="container">
          <div class="card mx-auto rounded" style="width: 400px; margin-top: 60px;">
            <div class="container row no-gutters">
              <div class="text-center">
                <div class="card-img" style="margin-top: 20px;"><%= image_tag @user.avatar, alt: 'avatar' %>
              </div>

                  <div class="text-center">
                    <div class="card-body">
                      <h5 class="card-title"><%= @user.first_name %> <%= @user.last_name %></h5>
                      <p class="card-text"><%= @user.email %></p>
                      <p class="card-text">"<%= @user.description %>." </p>

              <% if current_user.id == @user.id %>
                                        <a class="btn" href="<%= edit_user_registration_path %>">modifier mes infos</a>
                                                                  <% end %>
                    </div>
                  </div>

                                    <ul class="nav nav-pills nav-pills-icons" role="tablist">
                                      <li class="nav-item">
                                          <a class="nav-link active" href="#studio" role="tab" data-toggle="tab">
                                            <p>Events créés</p>
                                          </a>
                                      </li>
                                      <li class="nav-item">
                                          <a class="nav-link" href="#works" role="tab" data-toggle="tab">
                                            <p>Events rejoints</p>
                                          </a>
                                      </li>
                                    </ul>

          <div class="tab-content tab-space mx-auto">
            <div class="tab-pane active gallery" id="studio" style="padding: 10px;">
               <% @admin_events.each do |event| %>
                 <div class="mt-auto p-2">
              <div class="card">
                    <p class="card-header"><%= event.title %></p>
                    <a class="btn"><%= link_to "Voir", event_path(event.id), :class => 'card-link' %></a>
                  </div>
                </div>
            <% end %>
            </div>


            <div class="tab-pane gallery" id="works">

                <% @guest_events.each do |event| %>
                  <div class="mt-auto p-2">
                <div class="card">
                    <p class="card-header"><%= event.title %></p>
                    <a class="btn"><%= link_to "Voir", event_path(event.id), :class => 'card-link' %></a>
                  </div>
                </div>
                  <% end %>
                      </div>
</div>
</div>
  </div>
</div>
</div>
<br>
<br>
